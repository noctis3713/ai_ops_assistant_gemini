# Docker 容器環境變數配置檔案
# 此檔案專用於 Docker Compose 部署環境
# 複製此檔案為 .env 並填入實際的 API 金鑰值

# =============================================================================
# AI 服務配置 (必須設定)
# =============================================================================
# AI 提供者選擇: gemini 或 claude
AI_PROVIDER=gemini

# Google Gemini API 金鑰 (當 AI_PROVIDER=gemini 時必填)
GOOGLE_API_KEY=your_google_gemini_api_key_here

# Anthropic Claude API 金鑰 (當 AI_PROVIDER=claude 時必填)  
ANTHROPIC_API_KEY=your_anthropic_claude_api_key_here

# AI 模型配置
GEMINI_MODEL=gemini-1.5-flash-latest
CLAUDE_MODEL=claude-3-haiku-20240307

# =============================================================================
# 功能開關配置
# =============================================================================
# 啟用文檔搜尋功能 (建議關閉以節省 API 配額)
ENABLE_DOCUMENT_SEARCH=false

# 解析器版本 (original 或 enhanced)
PARSER_VERSION=original

# =============================================================================
# 系統管理配置
# =============================================================================
# 管理 API 金鑰 (用於配置重載等管理功能)
ADMIN_API_KEY=admin123

# =============================================================================
# 前端環境變數配置
# =============================================================================
# API 基礎 URL (Docker 環境中設定為內部服務名稱)
VITE_API_BASE_URL=http://backend:8000

# 前端運行環境
VITE_ENVIRONMENT=production

# =============================================================================
# 提示詞系統配置
# =============================================================================
# 提示詞語言設定
PROMPT_LANGUAGE=zh_TW

# 提示詞模板目錄 (容器內部路徑)
PROMPT_TEMPLATE_DIR=/app/templates/prompts

# =============================================================================
# 網路設備配置 (根據實際環境調整)
# =============================================================================
# 設備連線超時設定 (秒)
DEVICE_CONNECTION_TIMEOUT=30

# 指令執行超時設定 (秒)  
COMMAND_EXECUTION_TIMEOUT=20

# 最大並發連線數
MAX_CONCURRENT_CONNECTIONS=5

# =============================================================================
# 日誌配置
# =============================================================================
# 日誌級別 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 啟用詳細日誌 (true/false)
ENABLE_VERBOSE_LOGGING=false

# =============================================================================
# Docker 容器特定配置
# =============================================================================
# 後端服務端口 (容器內部)
BACKEND_PORT=8000

# 前端服務端口 (容器內部)
FRONTEND_PORT=80

# Nginx Proxy Manager 管理介面端口
PROXY_ADMIN_PORT=81

# HTTPS 端口 (對外)
HTTPS_PORT=443

# =============================================================================
# 資料持久化配置
# =============================================================================
# 日誌檔案存放路徑 (容器外部掛載)
LOGS_PATH=./logs

# 配置檔案存放路徑 (容器外部掛載)
CONFIG_PATH=./config

# SSL 憑證存放路徑 (容器外部掛載)
SSL_PATH=./letsencrypt

# Nginx Proxy Manager 資料存放路徑 (容器外部掛載)
PROXY_DATA_PATH=./proxy-data